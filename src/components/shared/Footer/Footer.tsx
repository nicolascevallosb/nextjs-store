import React from "react";
import styles from "./Footer.module.sass";

export const Footer = () => {
    return(
        <footer className={styles.footer}>
            <h1>Maxi Emporium</h1>
        </footer>
    );
}